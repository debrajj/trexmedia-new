[variables]
NIXPACKS_NODE_VERSION = "22"
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs-22_x", "npm-10_x"]

[phases.install]
cmds = ["npm ci --only=production --no-audit --no-fund"]

[phases.build]
cmds = ["npm run build"]

[phases.start]
cmd = "npm start"